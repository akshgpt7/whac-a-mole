name: Linter

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
   lint:
     runs-on: ubuntu-latest
     
     steps:
      - name: Testing with flake8
        uses: TrueBrain/actions-flake8@master
        with:
          path: .py 
